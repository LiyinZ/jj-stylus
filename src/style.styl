@import '../lib/jeet/stylus/jeet'
@import '_settings'

edit()

*
	margin 0
	padding 0
	font-family sans-serif
	
img
	col(1/4, cycle: 4)
	@media (max-width 600px)
		col(1/3, cycle: 3, uncycle: 4)
	
section
	clear both